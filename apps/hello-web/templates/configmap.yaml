apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hello-web.fullname" . }}
  labels:
    {{- include "hello-web.labels" . | nindent 4 }}
data:
  index.html: |
    <!DOCTYPE html>
    <html>
      <body>
        <h1>{{ .Values.message }}</h1>
      </body>
    </html>
