apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: pylon-integrations-staging-appset
  namespace: argo
spec:
  generators:
    - list:
        elements:
          - cluster: staging
            namespace: pylon-integrations-staging
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  template:
    metadata:
      name: pylon-integrations-{{.cluster}}
      labels:
        harness.io/envRef: <HARNESS_ENV_REF>
        harness.io/serviceRef: <HARNESS_SERVICE_REF>
    spec:
      project: mgmt-dev-usw2-ops-int-clusters
      source:
        repoURL: https://github.com/humzam-ditto/cloud-apps
        targetRevision: main
        path: apps/pylon-integrations
        helm:
          valueFiles:
            - values.yaml
            - values-staging.yaml
            - harness-release.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
  syncPolicy:
    applicationsSync: create-update
